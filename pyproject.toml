[project]
name = "studyflow-ai"
version = "0.1.0"
description = "From the intention to learn to real progress, guided by AI."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.ruff]
line-length = 100
target-version = "py310"
